@* Titled panel wrapper used across all pages for a consistent look. *@
<MudPaper Class="@($"section-card {Class}")" Elevation="0">
    @if (!string.IsNullOrWhiteSpace(Title) || Actions is not null)
    {
        <div class="section-card__head">
            <div class="section-card__heading">
                @if (!string.IsNullOrWhiteSpace(Title))
                {
                    <MudText Typo="Typo.overline" Class="i18n-both">@Lang.T(LangMode, Title)</MudText>
                }
                @if (!string.IsNullOrWhiteSpace(Subtitle))
                {
                    <MudText Typo="Typo.h6" Class="i18n-both">@Lang.T(LangMode, Subtitle)</MudText>
                }
                @if (!string.IsNullOrWhiteSpace(Help))
                {
                    <MudText Typo="Typo.body2" Class="helper-text">@Lang.T(LangMode, Help)</MudText>
                }
            </div>
            @if (Actions is not null)
            {
                <div class="section-card__actions">@Actions</div>
            }
        </div>
    }
    <div class="section-card__body">@ChildContent</div>
</MudPaper>

@code {
    [Parameter] public string? Title { get; set; }
    [Parameter] public string? Subtitle { get; set; }
    [Parameter] public string? Help { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter] public RenderFragment? Actions { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [CascadingParameter(Name = "LangMode")] public string? LangMode { get; set; }
}
