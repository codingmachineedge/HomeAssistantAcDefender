namespace HomeAssistantAcDefender.Services;

/// <summary>
/// 口語廣東話 — the Settings page: every panel title, setting label, and help string.
/// Keys are the EXACT English strings from Components/Pages/Settings.razor.
/// </summary>
public static class YueSettings
{
    public static void AddTo(Dictionary<string, string> map)
    {
        // Populated by the Cantonese translation pass (one entry per Settings.razor string).
    }
}
