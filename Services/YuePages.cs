namespace HomeAssistantAcDefender.Services;

/// <summary>
/// 口語廣東話 — page-level strings: Dashboard, Energy, Controls, Comfort, Logs, Guide, Defense
/// section titles, subtitles, helps, buttons, and table headers. Keys are the EXACT English
/// strings from the Components/Pages/*.razor files.
/// </summary>
public static class YuePages
{
    public static void AddTo(Dictionary<string, string> map)
    {
        // Populated by the Cantonese translation pass (one entry per page string).
    }
}
